#include<stdio>
#include<stdlib.h>
#include<string.h>
//memory alocation
int main(){
	char *lista[3]];
	int cont;
	char aux[20];
	int longitud;
	for(cont=0;cont<3;cont++){
		printf("palabra %d: ");
		scanf("%s",aux);
		longitud=strlen(aux);
		(char *)malloc(lomgitud);
	}
}
